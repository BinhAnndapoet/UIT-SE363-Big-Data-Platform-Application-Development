# ============================================================================
# train_eval_module - Python Dependencies
# ============================================================================
# This file contains all dependencies needed to train Text, Video, and Fusion
# models for TikTok Safety Content Detection.
#
# Usage:
#   1. Create virtual environment: python3 -m venv .venv
#   2. Activate: source .venv/bin/activate
#   3. Install: pip install -r requirements.txt
#
# Note: PyTorch with CUDA is recommended for faster training.
#       For CPU-only: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# ============================================================================

# === Core ML Frameworks ===
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.35.0
accelerate>=0.24.0
safetensors>=0.4.0
sentencepiece>=0.2.0
tokenizers>=0.15.0

# === Data Processing ===
pandas>=2.0.0
numpy>=1.24.0
datasets>=2.14.0
scikit-learn>=1.3.0
evaluate>=0.4.0

# === Video Processing ===
decord>=0.6.0
pillow>=10.0.0
opencv-python>=4.8.0

# === Audio Processing ===
librosa>=0.10.0
soundfile>=0.12.0

# === Visualization & Utilities ===
matplotlib>=3.7.0
seaborn>=0.13.0
tqdm>=4.65.0
wordcloud>=1.9.0
pyyaml>=6.0.0

# === Experiment Tracking ===
# Uncomment if using MLflow locally (optional)
# mlflow>=2.8.0

# === HuggingFace Hub (for model upload) ===
huggingface-hub>=0.19.0

# === Utilities ===
python-dotenv>=1.0.0
regex>=2023.10.0
