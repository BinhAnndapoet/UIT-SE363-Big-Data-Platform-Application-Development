# Spark Processor Python Dependencies
# NOTE: PyTorch CPU installed via Dockerfile Layer 3 from PyTorch index
# NOTE: transformers, decord, etc installed via Dockerfile Layer 4
# This file contains ADDITIONAL utilities only (Layer 5)

# Data/Storage clients
boto3==1.34.0
minio==7.2.0
kafka-python==2.0.2
psycopg2-binary==2.9.9

# Data processing
pandas==2.0.3

# Audio processing
librosa==0.10.1
soundfile==0.12.1
